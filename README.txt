run python file "roc_draw.py"
everything will be generated automatically
to run the algebra demonstrated install mkl on  a linux system, and from r install the rjava and matrix packages
to visualize the decision tree install the other packages noted
